import { VT323, Press_Start_2P } from 'next/font/google';

export const retro = VT323({
  subsets: ['latin'],
  display: 'swap',
  weight: '400',
});

export const pixelFont = Press_Start_2P({
  subsets: ['latin'],
  display: 'swap',
  weight: '400',
}); 